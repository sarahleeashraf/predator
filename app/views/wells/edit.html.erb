<h1>Editing well</h1>

<% form_for(@well) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :field_id %><br />
    <%= f.text_field :field_id %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @well %> |
<%= link_to 'Back', wells_path %>
